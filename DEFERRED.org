#+TITLE: Deferred & Dormant — ScribeShift
#+DATE: 2026-06-24

* Purpose
Forward-looking counterpart to PRODUCT_CONTEXT.org: intentionally dormant +
deferred features and their reactivation paths.

* Dormant Features (code present but not wired in)
| Feature | Why dormant | Reactivation path |
|---------|-------------|-------------------|
| (none) | — | — |

* Deferred (built but not cut over)
| Item | Reason for deferral | Trigger to revisit |
|------|---------------------|--------------------|
| Full worker-path generation cutover | T3-A landed the two-path end state: prompts are centralized in =server/config/skills.js= → =services/generation.js= → =services/gemini-client.js= (no longer duplicated), and the dead edge function + =useGenerationJob.js= hook are removed. The async path (=POST /api/generate/enqueue= → =generation_jobs= → =server/worker.js= on Cloud Run) is canonical for typed/URL jobs; the synchronous =/api/generate= route is retained for file uploads + as the enqueue-unreachable fallback. *Open preconditions before full cutover:* (a) a worker-health signal so the silent sync fallback can't mask a dead worker; (b) worker file-ingest, since file-upload jobs still hit the synchronous 60s cap (the worker has no file ingest). | Both preconditions met — add worker-health detection and worker-side file ingest; then route file jobs through the worker too. |

* Scheduled for Removal
| Item | Status | Note |
|------|--------|------|
| =src/components/BrandSetup.jsx=, =ContentTypeSelector.jsx=, =MultiFileUpload.jsx=, =StyleOptions.jsx=, =ui/card.jsx=, =ui/shape-landing-hero.tsx= | Deleted in working tree, not yet committed | Deliberate simplification pass; zero remaining cross-references. Safe to commit as deletes. |

* Open Product Questions
- /Resolved (T3-A)./ Which generation path is canonical? The async worker
  (=POST /api/generate/enqueue= → =server/worker.js=) is canonical for typed/URL
  jobs; the synchronous =/api/generate= route is retained for file uploads + as
  the enqueue-unreachable fallback. The edge function and =useGenerationJob.js=
  were removed. Remaining work is tracked under /Deferred/ above.

---
/Last Updated/: 2026-06-25
